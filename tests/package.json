{
  "name": "tests",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,functions \"node --experimental-vm-modules node_modules/jest/bin/jest.js .\"",
    "test:watch": "firebase emulators:exec --only firestore,functions \"node --experimental-vm-modules node_modules/jest/bin/jest.js --watch --verbose\"",
    "test:noemu": "GCLOUD_PROJECT=firebase-teams-jojo FIRESTORE_HOST=localhost FIRESTORE_PORT=8080 node --experimental-vm-modules node_modules/jest/bin/jest.js --watch"
  },
  "type": "module",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^2.0.5",
    "firebase": "^9.14.0",
    "jest": "^29.3.1",
    "firebase-functions": "^4.1.0"
  }
}
