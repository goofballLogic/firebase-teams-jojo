{
  "version": "1.0.0",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,functions,hosting,auth \"playwright test --project chromium\"",
    "test:headed": "firebase emulators:exec --only firestore,functions,hosting,auth \"playwright test --project chromium --headed\"",
    "dev:watch": "nodemon --watch . --exec \"playwright test --project chromium\""
  },
  "type": "module",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.28.1",
    "nodemon": "^2.0.20"
  }
}
